# Decision Field Safety / Agency Boundary

The framework may expose possibilities, constraints, consequences and exact transformations. It does not own a person's goals, values or authority.

```text
PERSON != MODEL
ASSISTANCE != OWNERSHIP
NAVIGATION != GOVERNANCE
TOOL_OUTPUT != HUMAN_DECISION
```

In scientific domains, a shared Decision Field structure is a modeling interface only until independently validated against that domain's laws/evidence.
