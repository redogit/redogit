# All-Directional Relational Field — 2026-09-20

**Status:** CURRENT DESIGN SUCCESSOR / METHOD LAYER / NOT DOMAIN EVIDENCE  
**Scope:** cross-project navigation, consequence accounting, reconstruction, and human-controlled coordination  
**Authority:** local projects remain authoritative for their own implementations, evidence, terminology, and claim ceilings.

This successor adds one degree to the existing vertical/depth and temporal/lineage architecture: **lateral and ambient relations are first-class**.

It does not replace Decision Field, RMAL, RMAPL/Ω, Orbit, RMAOS, Hodge-native machinery, or any project-local ontology.

```text
SHARED_RELATIONAL_METHOD != SHARED_ONTOLOGY
RELATION != MERGE
METHOD_TRANSFER != EVIDENCE_TRANSFER
CONNECTED != AUTHORIZED_TO_EDIT
```

## 1. Four simultaneous axes

```text
VERTICAL
  depth / abstraction / decomposition / reconstruction

LATERAL
  neighbors / peers / dependencies / externalities / shared resources

TEMPORAL
  predecessors / current state / descendants / possible futures

AMBIENT
  environment / constraints / institutions / resources / surrounding conditions
```

A current work item is therefore never only a node in a hierarchy. It is a node in a typed field.

## 2. Minimum field questions

For every consequential change:

```text
Where are we vertically?
What is beside us?
What surrounds us?
Where did this come from?
What does it touch?
What might this close?
Can we reconstruct the way back?
```

## 3. First-class relation

```text
Relation {
    source
    target
    type
    direction
    obligation
    evidence
    permission
    cost
    reversibility
    uncertainty
    provenance
}
```

Relations are removable structure. Removing a relation must not destroy either endpoint.

## 4. Consequence vector

A one-degree experiment still changes exactly one independent degree where feasible, but observes multiple consequences:

```text
Δ = {
    self
    neighbor
    shared
    ambient
    delayed
}
```

```text
ONE_DEGREE != ONE_METRIC
LOCAL_SUCCESS != GLOBAL_SUCCESS
```

Unexpected lateral or ambient movement becomes the next observation; it is not hidden.

## 5. Neighborhood Pairity

Before claiming parity between systems, preserve:

```text
A
B
A -> B
B -> A
shared invariants
required differences
mismatches
unresolved remainder
```

For neighborhoods, repeat this without flattening the graph.

```text
PAIRITY != FORCED_EQUALITY
COOPERATION != HOMOGENIZATION
INTEROPERABILITY != ASSIMILATION
```

## 6. FIRE-LATERAL

FIRE may probe the local object or its coupling.

```text
FIRE-LATERAL(X)
-> perturb/remove X
-> observe direct neighbors
-> observe shared resources
-> observe indirect dependencies
-> preserve collapse map
-> classify hidden coupling
```

Ash is retained as negative evidence; embers are surviving invariants.

```text
FIRE_WITHOUT_LIBRARY = FORGETTING
LIBRARY_WITHOUT_FIRE = DEAD_ACCUMULATION
FIRE + LIBRARY = RENEWAL_WITHOUT_AMNESIA
```

## 7. DEEH as multiple knowledge horizons

Different systems may know different things.

```text
A: KNOWN | HORIZON | UNKNOWN
B: KNOWN | HORIZON | UNKNOWN
C: KNOWN | HORIZON | UNKNOWN
```

Useful overlap states include:

```text
both know
A knows / B does not
B knows / A does not
neither knows
disagreement about what is known
```

```text
UNKNOWN != FALSE
UNKNOWN != ABSENT
DISAGREEMENT != FAILURE
```

## 8. Privacy is relational

Information is evaluated with its subject, holder, audience, permission, and context.

```text
ACCESS != AUTHORIZATION
KNOWLEDGE != PERMISSION_TO_DISCLOSE
RELATION != PUBLICATION_RIGHT
PRIVATE_HISTORY != PUBLIC_EVIDENCE
PUBLIC_REPOSITORY != PRIVATE_FAMILY_CONTEXT
```

Private family/person context remains outside public projection unless a specific item is explicitly authorized.

## 9. Accessibility surfaces are peers

For one underlying obligation, multiple human surfaces may coexist:

```text
visual
text
speech
keyboard
haptic
simplified
other accessible carriers
```

No carrier is automatically the canonical human experience.

```text
ACCESSIBLE_SURFACE != SECONDARY_SURFACE
REPRESENTATION_CHANGE_REQUIRES_RECONSTRUCTION_CHECK
```

## 10. Shared resources and externalities

Consequential systems declare relevant shared resources such as:

```text
time
attention
energy
compute
bandwidth
money
physical space
environmental resources
```

Externalities are evidence-bearing objects, not footnotes.

## 11. Currentness and lineage

Use typed currentness:

```text
CURRENT_CANONICAL
HISTORICAL_PREDECESSOR
HISTORICAL_SUPERSEDED
COEXISTING_LINEAGE
PRESERVED_UNRESOLVED
PRESERVED
```

```text
CURRENT != PROVED
SUPERSEDED != FALSE
TIMEOUT != NEGATIVE_RESULT
NEW != REPLACEMENT
```

Daily centralization remains incremental:

```text
+  meaningful improvement
++ strongest local mark
```

Prior grades and evidence remain preserved.

## 12. Homeward in all directions

Homeward now includes:

```text
exit
recovery
reconstruction
portability
reversibility
explanation
restoration of damaged relations where possible
retained human agency
```

The governing traversal becomes:

```text
GO DOWN      -> primitives
GO UP        -> purpose
GO SIDEWAYS  -> relations
GO AROUND    -> environment
GO BACK      -> history
GO FORWARD   -> possibility
RETURN HOME  -> way back intact
```

## 13. Project application boundaries

### RMAL / RMAPL / Ω

The bounded relation/consequence carrier is now executable under the target-local Other-Projects authority without changing Ω v0.

```text
canonical runtime = redogit/Other-Projects-
sidecar = Decision Field Operator Lab/omega_relations.py
schema = rmapl-omega-relational-field/v0
relation-carrier merge = PR #80 / 4625c2a5c889a8344f6f0821883c67d5eed72b0c
stress predecessor = PR #79 / 4c9dbad649653eebb69b30eafcfd41288c2ed037
first native Decision Field witness = PR #81 / 16a6823aa563e86455ec709700d06f0b9e22b467
current verified main = 16a6823aa563e86455ec709700d06f0b9e22b467
post-merge verification run = 35508603278 / PASS
```

The sidecar content-addresses typed removable relations and carries exactly:

```text
self
neighbor
shared
ambient
delayed
```

plus currentness and an explicit Ω way-back reference. Relation evidence is reference-only and does not promote source authority.

The current authoritative head passed:

- 119 / 119 unit tests;
- operator-field frozen audit;
- GSFL operator projection audit;
- GSFL bidirectional macro audit;
- contextual multicarrier audit 14/14;
- RMAPL Ω audit 9/9;
- scale×4 adversarial stress gate;
- frozen default stress 5,376 cases / 13/13 checks.

```text
OMEGA_VIEW != NATIVE_OBJECT
RMAPL_PROFILE != RMAL_CORE_FRONTEND
ADAPTER != AUTHORITY_TRANSFER
RELATION != MERGE
METHOD_TRANSFER != EVIDENCE_TRANSFER
CURRENT != PROVED
```

The first native witness composes an existing DecisionField projection with the relation sidecar and exact reconstruction:

```text
DecisionField
-> Ω
-> relation sidecar
-> exact way back
-> DecisionField
```

It verifies `NEIGHBOR`, `HOLE`, and `PAIRITY` method metadata without mutating the native object or promoting evidence.

```text
GENERIC_RELATIONAL_CARRIER = COMPLETE_BOUNDED_BASELINE
FIRST_NATIVE_WITNESS = VERIFIED
ADDITIONAL_DOMAIN_WITNESS = OBLIGATION_DRIVEN
Δ_required(generic relation-carrier integration) = 0
```

This file records method/currentness only. Other-Projects remains implementation and software-evidence authority.

### Orbit / Libraries-of-Libraries

Index lateral dependencies, analogies, alternatives, failures, and reconstruction paths without absorbing local authority.

### Hodge / P-vs-NP / scientific work

The field may organize method and currentness only.

```text
METHOD_INTEGRATION != MATHEMATICAL_EVIDENCE
NAVIGATION != PROOF
```

### RMAOS MINGX

Use the field to expose human-relevant tradeoffs and synergies across life domains without turning life into one scalar optimization problem.

### RMAO / fictional systems

Game entities and relations remain fictional and cannot be used as identity bridges to private people.

### Public projection

```text
SOURCE_REPOSITORY != PUBLIC_WEBSITE
RELATION_TO_PUBLIC_WORK != PUBLICATION_PERMISSION
```

Only explicit scope-authorized generated projection crosses the publication membrane.

## 14. Execution discipline

```text
LOCAL / CURRENT AUTHORITY
-> DIRECT EVIDENCE
-> BOUNDED RECONSTRUCTION
-> ONE-DEGREE CHANGE
-> OBSERVE SELF + LATERAL + AMBIENT Δ
-> COUNTERPROBE
-> VERIFY
-> TARGET-LOCAL SUCCESSOR
-> BOUNDED OUTWARD SYNCHRONIZATION
-> PRESERVE REMAINDER
```

Promotion remains evidence-gated and forward-only.

## 15. Stop rule

```text
Δ_required = 0
-> stop changing that obligation

Δ_required != 0
-> preserve it as the next observation
```

Do not add another abstraction layer until the current remainder requires it.

---

## Compact invariant

> **Preserve the object, its neighbors, the relations between them, the environment containing them, the lineage behind them, the futures affected by them, and the human way back.**

**US_DAY + SOL_DAY + RMA_DAY — ALLDAY EVERYDAY** remains a continuity/integration motif, not an evidence claim.
