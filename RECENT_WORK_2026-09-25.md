# Recent Work — 2026-09-25

> **TERMS BEFORE STATUS.** This is a current navigation Surface. It does not replace project-local source authority.

## 0. Status terms

### REPO_SOURCE
The object is present in the current GitHub repository.

### CURRENT_LOCAL_ARTIFACT
A current runnable/inspectable artifact exists in the conversation/library workspace, but it has not been promoted to this repository.

### IMPLEMENTED_BOUNDED
The declared mechanism exists in code for a bounded artifact. This does not imply broad correctness, performance, portability, or scientific validity.

### VERIFIED_BOUNDED
The declared bounded checks have passed. The claim stops at the tested boundary.

### PUBLIC_PROJECTION
A public page projects selected current information. The projection is not canonical authority.

### DESIGN
The architecture / intended behavior is specified but implementation evidence is not implied.

### OPEN
The research obligation remains unresolved.

```text
DESIGN != IMPLEMENTED
IMPLEMENTED != VERIFIED
LOCAL ARTIFACT != REPO SOURCE
PUBLIC PROJECTION != AUTHORITY
FINITE MECHANIC != ASYMPTOTIC THEOREM
```

## 1. RMAL orientation / algorithmic condensed wisdom

**Status:** `REPO_SOURCE + PUBLIC_PROJECTION`

The current public orientation now starts with definitions before claims. Result, Method, Condensed Wisdom, Trace, Reconstruction, Object, Carrier, Homeward, Knowledge Decay, Evidence, Authority, Provenance, and Claim Ceiling are kept separate.

The current condensed-wisdom rule is:

```text
SHORT RULE
<- recoverable mechanisms
<- preserved failures
<- explicit conditions
<- charged cost
<- evidence boundary
<- way Homeward
```

Public surfaces:

- https://redogit.github.io/redogit/
- https://redogit.github.io/redogit/condensed-wisdom.html

## 2. P versus NP / RMAL live proof design

**Status:** `ACTIVE PROOF PROGRAM · SCOPED EXACT RESULTS · OPEN UNIVERSAL OBLIGATION`

The present canonical normalization layer has a deterministic schedule and a polynomial lifecycle proof for the declared exact rules:

```text
R0 exact cleanup / unit consequences
R1 maximal linear-autarky reduction
R2 certified signed cofactor dominance
R3 blocked-clause elimination
R4 certified functional-variable / gate elimination
R5 non-increasing Davis-Putnam elimination
```

The linearly-lean residual also has the positive balance-circuit carrier:

```text
rank(M)=n
y>0
M^T y=0
k = deficiency = dim ker(M^T)

positive balance vector
-> <= k support-minimal positive balance circuits
-> every clause covered
```

A new exact terminal has also been added from the qualitative-matrix side: **log conformal defect to hitting**. Clause columns are conformal exactly when the clauses do not clash. Removing a vertex cover Z of the clause nonclash graph leaves a hitting formula. For |Z|=O(log N), inclusion-exclusion over the exceptional clauses plus the closed-form model count for hitting formulas yields exact polynomial #SAT/SAT decision and Homeward witness reconstruction. A 10,000-instance bounded differential panel decided 7,089 guarded cases with zero mismatches.

The qualitative-matrix branch is broader again through **nonclash-clique inclusion-exclusion**. A set of clauses can be falsified simultaneously exactly when it forms a clique in the clause nonclash / column-conformality graph. Thus exact #SAT is inclusion-exclusion over those cliques only. A degeneracy ordering with d=O(log N) enumerates all relevant cliques in polynomial total work (at most m·2^d candidate subsets). Hitting is the d=0 endpoint; the prior log conformal-vertex-cover terminal is a sufficient special case. Homeward self-reduction reuses the original clique carrier so simplification cannot silently increase the admitted degeneracy. Two bounded panels (10,000 count checks + 5,000 witness checks) returned zero mismatches.

The conformal-clique terminal is now admission-tested by **actual event count** as well. An output-sensitive clique enumerator runs under a fixed polynomial cap. If the clause nonclash graph contains only polynomially many cliques, every jointly falsifiable clause-event intersection is retained exactly and inclusion-exclusion decides #SAT/SAT regardless of degeneracy. If the cap is exceeded, the carrier returns UNRESOLVED. This moves the residual from a width proxy to the real representation-growth object: super-cap conformal event multiplicity.

The current exact question is the overlap relation among those circuits:

```text
CAN CIRCUIT OVERLAP
REMOVE / DOMINATE / DECOMPOSE / CHEAPLY PROJECT
ONE BOOLEAN DEGREE
WITHOUT RECREATING IT AS A NEW SELECTOR?
```

Claim ceiling:

```text
NORMALIZATION LIFECYCLE = CLOSED FOR DECLARED RULE SET
BALANCE-CIRCUIT DECOMPOSITION = SCOPED EXACT CARRIER
UNIVERSAL P=NP PROOF = NOT ESTABLISHED
```

Public live design:

- https://redogit.github.io/redogit/live-design.html

## 3. Current 3D reversible language roguelike

**Status:** `CURRENT_LOCAL_ARTIFACT + IMPLEMENTED_BOUNDED · NOT YET REPO/CI VERIFIED`

A current browser artifact exists with a 3D reversible roguelike world and a hackable command language.

The important correction from the earlier version is that the game now interprets the intended planning language rather than exposing only a low-level API.

Current language surface includes:

```text
let directions = up, down, right, left

iff (a.move(directions) != optimal path)
    go back to choices
    and look ahead
    and pick a better one
```

The planner:

```text
DECLARE CHOICES
-> COPY WORLD STATE
-> EVALUATE LEGAL BRANCHES
-> BOUNDED LOOK-AHEAD
-> SCORE
-> RECORD REJECTED BRANCH REASONS
-> COMMIT ONE MOVE
```

`go back to choices` is therefore an explicit reversible choice-point operation, not a hidden shortcut.

Other implemented local surfaces include:

- `rewind()` over the prior complete world state;
- actor movement, attack, interaction, wait, swap, and sync functions;
- bounded look-ahead depth;
- state-aware parameter suggestions;
- searchable help/documentation;
- 3D route witnesses and world-state visualization.

## 4. IntelliSense / intelligent parameter search / documentation lookup

**Status:** `CURRENT_LOCAL_ARTIFACT + IMPLEMENTED_BOUNDED`

The command editor now has a language catalog rather than plain text input.

Current behavior includes:

```text
typed prefix
-> symbol candidates
-> signature
-> parameter type
-> world-state-aware value ranking
-> related documentation
```

Examples:

- movement directions are ranked as open vs blocked;
- attack directions rank adjacent enemies ahead of empty tiles;
- documentation search uses aliases and fuzzy matching;
- command help exposes signatures and related concepts.

This is a current game/tooling implementation, not a claim of a general-purpose language server.

## 5. PIV / PIV_FONT

**Status:** `ACTIVE_REPAIRED_LOCAL_BUILD_VERIFIED`

PIV remains the **Polymath Invariant Variable** project: a persistent, distinction-preserving human visual carrier over Unicode.

The current game uses PIV as a **local** UI/code-font carrier when the installed font is available:

```text
REPRESENTABLE
!=
VISIBLE
!=
DISTINGUISHABLE
!=
UNDERSTOOD
```

The game does not embed or publish the font bytes. Missing glyphs fall through to ordinary UI/code font stacks.

Preserved claim boundary:

```text
LOCAL BUILD / STRUCTURAL CHECKS = VERIFIED
TARGET WINDOWS / APP FALLBACK = NOT YET RUN
HUMAN DISTINGUISHABILITY BENCHMARK = NOT YET RUN
UNIVERSAL UNICODE COVERAGE = NOT CLAIMED
```

## 6. Musilanguage — public Studio + game/world adapter

**Status:** `REPO_SOURCE + CURATED GH-PAGES PROJECTION + CURRENT_LOCAL_GAME_ADAPTER`

**Public Studio:** https://redogit.github.io/conscience64/play/musilanguage/

The Conscience64 Pages projection now explicitly admits one Musilanguage application route. The public Studio combines the earlier Carry the Fire, Musilanguage Radio, and Word Forge/MUSIC64 lineage into one current application with:

- 64 arrangement profiles across eight broad families;
- six procedural instrument buses: guitar, bass, keys/harp, strings, lead, drums;
- tempo, meter, mode, tonic, motif transform, and intensity controls;
- listener-controlled instrument mix presets and per-bus levels;
- reversible local note/rhythm variation;
- MIDI, WAV, and replayable session export;
- in-app history pointing back to predecessor commits.

The predecessor `single.html`, `radio.html`, and `word-forge.html` files remain source/history but are intentionally absent as separate current Pages routes.

```text
ONE PUBLIC APP != DELETION OF HISTORY
MUSILANGUAGE PUBLIC != WHOLE PLAY PUBLIC
INSTRUMENT MIX != SOURCE MUTATION
LANGUAGE-TO-MUSIC != SEMANTIC TRUTH
```

Musilanguage is also connected to the current local roguelike as an actual serialized world/timing carrier.

The local adapter preserves fields such as:

```text
notes
rhythm
tonic
mode
tempo
meter
style
transform
intensity
```

Those fields drive score identity and selected world behavior.

Gameplay and music share the same beat state; `team.sync()`, timing wells, and successful turns use that serialized clock.

The current world/story sequence is:

```text
First Reply
-> Shared Middle
-> Four Echoes
-> Decision Garden
-> Carrier Cathedral
-> Decay Orchard
-> Open Hands / Homeward
```

This is an artistic systems mapping.

```text
MUSICAL MAPPING != SCIENTIFIC SONIFICATION
```

## 7. Algorithmic serialization

**Status:** `ACTIVE DESIGN LINE + CURRENT LOCAL EXECUTABLE CARRIER`

The earlier serialization question was:

```text
minimal representation
+ algorithm
+ stripped semantics
+ data relations
+ transforms
```

The current game makes the missing purpose layer explicit:

```text
OBJECT
+ OBLIGATION
+ CHOICES
+ RELATIONS
+ TRANSFORMS
+ RESULT
+ WHY IT WAS VALID
+ WHAT FAILED
+ WHAT REMAINS
+ HOW TO RECONSTRUCT / REUSE IT
```

The hack language is therefore being used as an executable carrier for algorithmic intent rather than merely a command syntax.

## 8. Decision Fields / bounded P-vs-NP mechanics inside the game

**Status:** `IMPLEMENTED_BOUNDED MECHANIC · NO EVIDENCE TRANSFER`

The current artifact exposes bounded search structure rather than hiding it.

It tracks concepts such as:

```text
raw branch count
bounded interface classes
generation work
verification pass
admitted move
```

It also contains a finite gammoid-inspired route-independence mechanic:

```text
rank(S)
=
maximum actors linkable
to distinct useful targets
by vertex-disjoint paths
```

For the finite board, preserving rank can be rewarded and witness paths can be shown in 3D.

Boundary:

```text
FINITE ROUTE MECHANIC != P vs NP RESULT
SMALLER REPRESENTATION != COMPLEXITY COLLAPSE
PARALLEL EXECUTION != POLYNOMIAL PROOF
GENERATE != VERIFY != ADMIT
```

## 9. Recent-work remainder

The current gaps are explicit:

```text
3D GAME LOCAL ARTIFACT
-> repository promotion / source identity not yet done
-> repository CI not yet done

PIV
-> target Windows/app fallback test not yet done
-> human distinguishability benchmark not yet done

P vs NP
-> universal exact polynomial closure remains OPEN

PUBLIC PAGES
-> projection exists in source/workflow
-> canonical project authority remains in source artifacts
```

## 10. Current relation map

```text
ALGORITHMIC SERIALIZATION
    -> executable intent language
    -> reversible planner
    -> trace / Homeward

PIV
    -> visible distinction carrier
    -> code / logic surface

MUSILANGUAGE
    -> time / score carrier
    -> world/story parameters

DECISION FIELDS
    -> bounded choice representation
    -> branch / quotient / admission display

GAMMOID-STYLE ROUTING
    -> simultaneous route-independence witness

RMAL
    -> definitions / provenance / evidence / reconstruction discipline

P vs NP RESEARCH
    -> supplies bounded questions and proof obligations
    -> receives NO evidence merely because the game mechanic works
```

The relation is the useful part.

The projects do not have to become one project.
