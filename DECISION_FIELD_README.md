# Decision Field

Start here: [`DECISION_FIELD_CANONICAL_POINTER.md`](DECISION_FIELD_CANONICAL_POINTER.md).

Core:

```text
DF = (X, O, D, R, F, E, G, U)
```

One field plus typed functions transforms the current possibilities toward a desired bounded result while preserving declared invariants, provenance and claim boundaries.
