# Cubic Paired-NAE Bond-Lattice Carrier — 2026-09-26

**Predecessor:** PNP_CUBIC_3UNIFORM_RESIDUAL_2026-09-26.md  
**Program:** RMAL / P vs NP  
**Status:** scoped exact theorem + bounded differential validation  
**Claim ceiling:** not a universal P=NP proof

## 1. Why this carrier exists

For a 3-uniform hypergraph H=(V,E), the paired CNF is

    F_H
      =
    AND_{e={a,b,c} in E}
      [(x_a OR x_b OR x_c)
       AND
       (not x_a OR not x_b OR not x_c)].

The generic semantic-intersection carrier treats the two falsifying directions separately:

    edge e all-0
    edge e all-1.

That is exact, but it forgets that these two clause events came from one original obligation:

    edge e must not be monochromatic.

For the paired NAE family, preserve that obligation as one Object.

## 2. Monochromatic-edge events

For every hyperedge e define

    Mono(e)
      =
    { colorings in which every vertex of e has the same color }.

Then:

    proper 2-coloring of H
      iff
    coloring avoids every Mono(e).

For A subseteq E, intersecting the events Mono(e), e in A, does not fix a truth value.

It imposes only equality:

    all vertices joined through selected hyperedges
    must receive the same color.

Let pi_A be the partition of V into connected components of the selected-edge subhypergraph (isolated vertices included as singleton blocks).

Then:

    intersection_{e in A} Mono(e)

depends only on pi_A, and its size is

    2^(|pi_A|).

Thus many edge-subset histories may denote one exact equality event.

## 3. Equality-partition / bond-lattice quotient

Let L_H be the smallest set of partitions of V containing the discrete partition and closed under:

    pi
      -> 
    join(pi, e),

where join(pi,e) merges every current block meeting hyperedge e.

Equivalently:

    L_H
      =
    { pi_A : A subseteq E }.

This is the hypergraph bond / intersection partition poset associated with the monochromatic-edge subspace arrangement.

Order partitions by refinement:

    pi <= rho
    iff
    pi refines rho.

Distinct subsets A,A' with

    pi_A = pi_A'

are one semantic event Object.

    EDGE-SUBSET HISTORY != EVENT IDENTITY
    EVENT IDENTITY = EQUALITY PARTITION

## 4. Möbius exact 2-color count

Let mu be the Möbius function of L_H from the discrete partition hat0.

Then the hypergraph chromatic polynomial satisfies

    P_H(q)
      =
    sum_{pi in L_H}
      mu(hat0,pi) q^(|pi|).

Therefore the original paired NAE obligation is exactly:

    #NAE(F_H)
      =
    P_H(2)
      =
    sum_{pi in L_H}
      mu(hat0,pi) 2^(|pi|).

Hence:

    P_H(2) > 0
      -> SAT / Property B

    P_H(2) = 0
      -> UNSAT / non-2-colorable.

External alignment: Jacob A. White, "On Multivariate Chromatic Polynomials of Hypergraphs and Hyperedge Elimination", Electronic Journal of Combinatorics 18(1), P160 (2011), DOI 10.37236/647, gives the hypergraph chromatic polynomial through Möbius inversion on the hypergraph partition/bond lattice.

## 5. Output-sensitive polynomial admission

Let

    J_eq(H) = |L_H|.

Fix a declared polynomial cap

    P(N_input)=N_input^c

for constant c.

Construct L_H incrementally in stable hyperedge-ID order:

1. start with the discrete partition;
2. for every retained partition pi, form join(pi,e);
3. canonicalize and deduplicate the resulting partition;
4. if the number of distinct partitions exceeds P(N_input), stop and return UNRESOLVED.

After processing the first i hyperedges, the retained set is exactly the partitions generated by subsets of that prefix.

Therefore if construction completes under the cap:

- event discovery is polynomial;
- each partition uses O(n) coordinates;
- refinement tests are polynomial;
- Möbius recurrence is polynomial;
- exact integer arithmetic uses polynomially many bits.

Thus:

    POLYNOMIAL DISTINCT EQUALITY-PARTITION COUNT
        ->
    EXACT #2-COLOR / SAT / UNSAT
    IN POLYNOMIAL TOTAL WORK.

## 6. Homeward witness without rebuilding the carrier

Retain L_H and its Möbius coefficients.

For a partial vertex coloring alpha, a partition pi contributes zero when one block of pi contains vertices assigned both colors by alpha.

Otherwise let

    fixed_alpha(pi)

be the number of blocks of pi containing at least one alpha-assigned vertex.

Then the number of full colorings extending alpha and lying in the equality event pi is

    2^(|pi|-fixed_alpha(pi)).

Therefore:

    #proper extensions of alpha
      =
    sum_{pi compatible with alpha}
      mu(hat0,pi)
      2^(|pi|-fixed_alpha(pi)).

Self-reduction can assign vertices in stable-ID order using the original partition carrier:

    try color 0;
    if extension count > 0 keep 0;
    otherwise use 1.

At most n calls are required.

The carrier guard cannot worsen during Homeward because L_H is never rebuilt.

## 7. Strict improvement over split signed events on retained witnesses

The existing cubic calibration witnesses give:

| instance | generic signed semantic states | equality-partition states | #2-colorings |
|---|---:|---:|---:|
| Fano | 73 | 37 | 0 |
| full-rank SAT-7 | 69 | 35 | 14 |
| SAT girth-6 n=10 | 495 | 227 | 48 |

The equality carrier is smaller on all three retained examples.

This does not prove a universal asymptotic bound.

The reason for the reduction is semantic, not empirical:

    ALL-0(e) OR ALL-1(e)
        ->
    MONOCHROMATIC(e)
        ->
    EQUALITY CONSTRAINT,

which is the original NAE hyperedge obligation.

## 8. Bounded differential validation

A separate checker generated 1,000 random simple cubic 3-uniform hypergraphs with n from 6 through 9.

For every instance it compared:

    Möbius count on L_H

against

    exhaustive 2^n vertex-color enumeration.

It also tested one random partial coloring per instance using the Homeward extension-count formula.

Results:

    tested cubic instances: 1,000
    total-count mismatches: 0
    partial-extension mismatches: 0
    largest observed equality-partition state count: 149.

This is bounded implementation evidence only. Sections 2-6 are the theorem.

## 9. Relation to the one-degree rule

The generic semantic carrier changed one clause-sign event at a time.

The paired carrier changes one original hyperedge obligation at a time:

    add Mono(e)
      ->
    merge exactly the equality blocks touched by e.

No truth value is chosen.

No Boolean selector is introduced.

The coordinate being added is an equality relation forced only inside the bad-event intersection carrier.

## 10. Updated cubic remainder

The current cubic paired residual should now be carried simultaneously as:

    cubic 3-uniform hypergraph H
    +
    cubic bipartite incidence graph G_I
    +
    paired positive balance circuits
    +
    semantic signed intersection quotient L(F_H)
    +
    equality-partition bond lattice L_H
    +
    edge-critical deletion structure when non-2-colorable.

The equality carrier closes every instance with polynomial J_eq(H).

A surviving cubic component must therefore exceed the admitted polynomial equality-partition cap, in addition to surviving all earlier normalization/terminal carriers.

    SUPER-CAP SIGNED STATES
    !=
    SUPER-CAP EQUALITY STATES.

Do not promote the former after the latter remains small.

## 11. Next exact target

The next useful degree is:

    EDGE-CRITICAL CUBIC 3-UNIFORM
    +
    SUPER-CAP EQUALITY-PARTITION MULTIPLICITY.

Search for a polynomially recognizable incidence structure that:

- factors the partition lattice;
- yields a bounded frontier;
- gives deletion/contraction dominance;
- gives a compact chromatic-polynomial recurrence;
- or produces a direct positive/negative certificate.

Do not return to generic recoloring coordinates.

GENERATE != VERIFY != ADMIT.

SCOPED CHROMATIC CARRIER != UNIVERSAL P=NP PROOF.


## 12. Signed partition-coefficient transfer carrier

The full bond lattice L_H is an exact semantic carrier, but it still retains every generated equality partition even when the total inclusion-exclusion coefficient of that partition has already canceled to zero.

The next one-degree repair is to carry the signed aggregate coefficient **during** hyperedge processing and retain only nonzero partition states.

Fix the stable hyperedge order

    e_1,...,e_m.

For a prefix [i], define

    c_i(pi)
      =
    sum_{A subseteq {e_1,...,e_i} : pi_A=pi}
      (-1)^|A|.

Thus c_i(pi) is the complete signed weight of every processed-edge history that has the same current equality-partition Object pi.

### 12.1 Exact transition

Initialize

    c_0(hat0)=1

for the discrete partition hat0.

When processing e_i, each existing history has exactly two inclusion-exclusion directions:

    -e_i : do not include e_i
    +e_i : include e_i.

At the partition level this gives:

    new[pi]          += c_(i-1)(pi)
    new[join(pi,e_i)] -= c_(i-1)(pi).

After aggregating equal partitions, delete every state whose new coefficient is exactly zero.

### 12.2 Inductive correctness

Assume c_(i-1) has the stated subset-sum meaning.

Every subset A of the first i hyperedges is uniquely either:

    A'                       with e_i absent

or

    A' union {e_i}           with e_i present.

The absent subset contributes

    (-1)^|A'|

to pi_(A').

The present subset contributes

    -(-1)^|A'|

to join(pi_(A'),e_i).

Therefore the transition produces exactly

    c_i(rho)
      =
    sum_{A subseteq [i] : pi_A=rho}
      (-1)^|A|.

No history is lost; only histories with the same semantic equality state are algebraically combined.

### 12.3 Zero-state deletion is exact

Suppose after aggregation:

    c_i(pi)=0.

Every future hyperedge operation is linear in this coefficient:

    carry contribution       = c_i(pi)
    join contribution        = -c_i(pi).

Both are zero.

So the histories represented by pi have no net contribution to **any future coefficient state**.

The state may be removed permanently from the active carrier.

A partition with the same identity can still be generated later from another nonzero state; that future contribution is retained normally.

Thus:

    ZERO AGGREGATE COEFFICIENT
        ->
    NO FUTURE CONTRIBUTION FROM CURRENT HISTORY CLASS.

This is cancellation, not semantic deletion.

### 12.4 Exact final count

After all m hyperedges:

    c_m(pi)
      =
    sum_{A subseteq E : pi_A=pi}
      (-1)^|A|.

Grouping the Whitney-Tutte-Fortuin-Kasteleyn expansion by equality partition gives:

    P_H(q)
      =
    sum_pi c_m(pi) q^(|pi|).

In particular:

    #2-colorings
      =
    P_H(2)
      =
    sum_pi c_m(pi) 2^(|pi|).

For the complete bond lattice these aggregate coefficients coincide with the Möbius coefficients used in Sections 3-4.

### 12.5 Coefficient bit size

For prefix i:

    |c_i(pi)| <= 2^i.

Therefore every exact coefficient requires at most

    i+1 <= m+1

bits up to sign.

Large arithmetic values do not hide exponential bit complexity.

### 12.6 Active coefficient width

Define

    W_coeff(H, order)
      =
    max_i |supp(c_i)|,

where supp(c_i) is the set of partitions with nonzero coefficient after prefix i.

Fix one reproducible hyperedge order by stable IDs.

Fix an admitted polynomial cap

    P(N_input)=N_input^c.

Run the signed transfer fail-closed:

    if active nonzero state count exceeds P(N_input):
        stop
        return UNRESOLVED under this carrier;

    otherwise:
        continue exactly.

There are m rounds.

Each active state creates only two transition contributions, and partition join/canonicalization is polynomial.

Hence:

    W_coeff <= P(N_input)
        ->
    exact polynomial chromatic / 2-color counting.

No search for an optimal edge order is assumed.

A separately certified low-width order may be admitted, but order discovery is charged independently.

### 12.7 Homeward under the retained final coefficient map

For a partial 2-coloring alpha and a retained partition pi:

- if one block contains vertices fixed to both colors, contribution is zero;
- otherwise let fixed_alpha(pi) be the number of blocks touched by alpha.

Then:

    extensions(pi,alpha)
      =
    2^(|pi|-fixed_alpha(pi)).

So:

    #proper extensions of alpha
      =
    sum_pi
      c_m(pi)
      * extensions(pi,alpha).

Self-reduction uses the same final coefficient map.

The carrier is not rebuilt and the width guard cannot worsen during witness reconstruction.

### 12.8 Relationship to standard chromatic expansions

The transition is the streaming form of the Whitney-Tutte-Fortuin-Kasteleyn subset expansion:

    P_H(q)
      =
    sum_{A subseteq E(H)}
      (-1)^|A| q^(components(V,A)).

The present contribution is the proof-program carrier choice:

    subset histories
      ->
    equality-partition Object
      ->
    aggregate exact signed coefficient
      ->
    delete zero coefficient Objects.

This is a representation/algorithm theorem, not a new chromatic-polynomial identity.

### 12.9 Retained calibration witnesses

Using the stable edge order recorded in the cubic residual artifacts:

| instance | full equality states | final nonzero coefficient states | maximum active coefficient width | #2-colorings |
|---|---:|---:|---:|---:|
| Fano | 37 | 37 | 37 | 0 |
| full-rank SAT-7 | 35 | 32 | 32 | 14 |
| SAT girth-6 n=10 | 227 | 226 | 226 | 48 |

So cancellation is real, but is not claimed to be universally large.

### 12.10 Bounded differential validation

A separate checker generated 1,000 random connected simple cubic 3-uniform hypergraphs with 6-9 vertices.

For every instance it compared:

    signed partition-coefficient transfer count

against:

    exhaustive 2^n color enumeration.

Results:

    tested: 1,000
    count mismatches: 0.

The strongest observed state reduction in this panel was:

    full equality-partition states: 139
    final nonzero coefficient states: 100
    maximum active coefficient width: 100
    exact #2-colorings: 54.

Witness hyperedges:

    (1,5,8)
    (2,6,7)
    (2,3,8)
    (0,1,5)
    (0,1,4)
    (4,7,8)
    (0,5,7)
    (3,4,6)
    (2,3,6)

Finite validation is implementation evidence only.

### 12.11 Updated cubic remainder

The full bond-lattice size is now only a sufficient upper bound on execution state.

The tighter execution resource is:

    ACTIVE NONZERO PARTITION-COEFFICIENT WIDTH

under a fixed, charged hyperedge order.

Therefore the live cubic residual becomes:

    EDGE-CRITICAL CUBIC 3-UNIFORM
    +
    SUPER-CAP ACTIVE PARTITION-COEFFICIENT WIDTH

after all earlier terminals.

This is still a scoped representation-growth obstruction, not a hardness certificate.

The next one-degree repair should project equality coordinates that can no longer affect any unprocessed hyperedge, rather than changing truth values.


## 13. Executable receipts for the signed coefficient carrier

Executable checker:

    PNP_CUBIC_BOND_COEFFICIENT_CHECK_2026-09-26.py

Bounded evidence:

    PNP_CUBIC_BOND_COEFFICIENT_EVIDENCE_2026-09-26.json

The checker reconstructs the full equality-partition carrier and the nonzero signed-coefficient carrier independently, compares exact counts to exhaustive 2-coloring on the bounded panels, and reports the active-width reduction.

The JSON evidence remains:

    BOUNDED_VALIDATION_ONLY.

The theorem authority is Section 12.
